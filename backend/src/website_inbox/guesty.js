'use strict';

// Compatibility shim. Guesty is an org-wide FAD integration, not owned by
// website_inbox. New code should import from `../integrations/guesty`.

module.exports = require('../integrations/guesty');
